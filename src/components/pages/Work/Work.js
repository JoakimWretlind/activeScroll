import { SharedSection, MainHeader } from "../../sharedStyling"

const Work = ({ forwardedRef }) => {
    return (
        <SharedSection id="work" className="" ref={forwardedRef}>
            <MainHeader>work</MainHeader>
        </SharedSection>
    )
}

export default Work